DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
JWT_SECRET="<strong-random-secret>"
OTP_SECRET="<otp-generation-secret>"

# Optional runtime config
NODE_ENV="development"

# Mail transport (used by legacy helper path until full mail service migration)
MAIL_USER=""
MAIL_PASS=""
MAIL_FROM="Matrix Intertech <no-reply@example.com>"

# Twilio (stubbed in current phase; keep empty)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_CHAT_SID=""
TWILIO_API_SID=""
TWILIO_API_SECRET=""
